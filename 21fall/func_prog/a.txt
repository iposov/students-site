data Complex a = (:+) a a

Num
  (+), (*), abs, signum, fromInteger, (negate | (-))
  Double Float Int Integer Natural RealFloat a => Complex a

(RealFrac a, Floating a) => RealFloat a
  atan2, isNaN, ...

(Real a, Fractional a) => RealFrac a
